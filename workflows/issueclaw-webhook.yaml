# GitHub Actions workflow for the TARGET repo (e.g., gigaverse-app/linear-git).
# Copy this file to .github/workflows/issueclaw-webhook.yaml in your target repo.
#
# Required repository secrets:
#   LINEAR_API_KEY - Linear API key for fetching entity data
#
# Triggered by repository_dispatch from the CF Worker webhook proxy.

name: Apply Linear Webhook

on:
  repository_dispatch:
    types: [linear-webhook]

permissions:
  contents: write

jobs:
  apply-webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install issueclaw
        run: pip install issueclaw

      - name: Apply webhook payload
        env:
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
          WEBHOOK_PAYLOAD: ${{ toJson(github.event.client_payload) }}
        run: issueclaw apply-webhook

      - name: Commit and push changes
        run: |
          git config user.name "issueclaw-bot"
          git config user.email "issueclaw-bot@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "sync: apply Linear webhook (${{ github.event.client_payload.type }} ${{ github.event.client_payload.action }})"
            git push
          fi
