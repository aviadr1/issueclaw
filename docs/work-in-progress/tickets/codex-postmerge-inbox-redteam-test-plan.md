# Post-merge identity and readiness loop

Live evidence: the authorized consumer run 34752722340 failed before claim because inbox credentials were absent. A fresh read confirms they remain absent. No repeat dispatch until prerequisites change. Live verification of a fix is blocked on deployment/configuration, not replaced by local tests.

## Batch A: identity ownership

- [ ] Deletion matrix: Issue/Project/Initiative/Document through direct webhook and isolated production preparation; all aliases removed, unrelated entity unchanged. No external API needed.
- [ ] Removing one alias retains a valid reverse lookup.
- [ ] Repeated pull title changes converge to one path; real parser/renderer/filesystem, only Linear API substituted.
- [ ] Shared writer refuses foreign ownership and conflicting historical content before mutation.
- [ ] Re-run the real mirror sample reproducer on temporary copies only.
- [ ] Full Python suite, lint/types; CI on frozen commit while investigating readiness batch.

## Batch B: CI readiness

- [ ] Missing/invalid configuration rejected before checkout/install; execute actual workflow preflight across a parameter matrix, not text matching alone.
- [ ] Evaluate shallow checkout through real Git replay tests before changing depth.

Invariant owner: SyncState owns identity mappings and canonical file replacement/removal for pull and webhook refresh. Divergent historical aliases must not be silently discarded during refresh. No bulk live migration is authorized or performed.
